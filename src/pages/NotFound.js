function NotFound() {

    return (
        <section className="page">
            <section className="not-found subpage">
                <img src="./images/Farmer.png" />
                <p>Sorry Pardner, can't find your crop!</p>
                <p>Check in another field!</p>
            </section>
        </section>
    )
}

export default NotFound;